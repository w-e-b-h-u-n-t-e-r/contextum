<!-- Generated by Contextum v0.1.0. Maturity: auto_generated — needs human review. -->

# Context Lifecycle

Trust state of this context layer.

| State | Meaning |
| --- | --- |
| auto_generated | created by tool/agent — needs review |
| reviewed | a human or architect reviewed it |
| trusted | repeatedly validated and used |
| stale | known outdated |
| deprecated | no longer maintained |

**Current state: `auto_generated`.**

Promote the state as the context is reviewed and proven. Demote to `stale`
when the code drifts ahead of the context.

## Current review note

On 2026-06-03, an AI agent inspected the repository source and replaced the
initial placeholders with verified facts where evidence exists. This is still
not human-reviewed; keep `context_maturity.level` as `auto_generated` and
`needs_human_review: true` until an authorized human review happens.
